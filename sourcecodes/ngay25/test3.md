# Xây dựng 1 class Hình ảnh
```
với các thuộc tính
width, height, alt của hình ảnh
1 method constructor với các tham số width, height, alt
1 phương thức render ra html của hình ảnh
khi call method render() như
$image->render() thì hình ảnh sẽ in ra trong trang html
```